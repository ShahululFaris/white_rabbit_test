/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RootWindow from '../picaday/app/Views/RootWindow'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RootWindow);
